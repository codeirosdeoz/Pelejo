name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macOS-latest
      - name: Checkout repository
        uses: actions/checkout@v1
      - name: Perform build
        run: xcodebuild